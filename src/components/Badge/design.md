# Badge

计数、上限和小圆点。无默认插槽时为行内计数；默认插槽存在时角标定位。

默认 size 继承 Form / 全局 nm；共用 Control Recipe：controlHeight / fontSize / padding 为 0，gap / radius 为 -1，icon 为 0。支持自定义有序尺寸和 ±1 级迁移，边界 clamp。颜色采用 Material 3 语义配对。
