# Switch

可访问原生 checkbox + switch 角色；状态位移遵循 Motion。

内置 Label，通过 labelOption 配置；继承 Form 的 disabled/required/error/size。默认 nm，controlHeight/font/padding 偏移 0、gap/radius -1，边界 clamp。原生属性转交控件，支持 aria-label。
