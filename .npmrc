# Vue 3.6 is currently prerelease. @vitejs/plugin-vue supports Vapor,
# but npm's semver peer resolver does not include prereleases in ^3.2.25.
# Remove this once Vue 3.6 stable is adopted.
legacy-peer-deps=true
